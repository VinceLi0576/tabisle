# 版本号怎么走，1.0 是什么

📌 老徐 260915 问「为何是 0.11.0，我们是否可以对齐一下」⇒ 这份就是答案。他说「你自己安排」，所以规矩由我定，写在这儿备查。

## 三位数，从右往左看

```
0 . 11 . 0
│    │    └─ 修 bug、调样式、改文案、补测试            → 最后一位 +1
│    └────── 加了一块用户能看见的新东西                → 中间一位 +1，最后一位归零
└─────────── 够格发布了                                → 0 变 1
```

**中间一位 +1 的例子**：顶部工作台四页合一 · 文件夹独立一套标签 · 标签管理页。
**最后一位 +1 的例子**：修同步丢键 · 换八色标准 · 调侧栏布局 · 字号收敛。

判据一句话：**这次改动要不要跟他解释「这是个新东西」** —— 要，就动中间那位。

## 1.0 的五道门槛

到齐了才把第一位从 0 改成 1。🔴 **每一条都要真跑过一次，不是看代码觉得没问题。**

- [ ] **① 没有已知的静默丢数据**
      `260915` 一天就修了 6 条这类（同步整键删掉 meta 子键、改名丢颜色和状态、右键写错字段…）。
      判据：这一类的测试全绿，而且**测试本身验过有效**（故意改坏一处，它要当场报）。
- [ ] **② 双机同步真跑过一轮完整的**
      两台浏览器真对拷一遍：各自改一批 → 同步 → 看两边是不是都对。
      🚫 只跑单元测试不算 —— `260915` 那个「每同步一次删两个键」的 bug，单元测试一条都没报。
- [ ] **③ 备份能恢复**
      真恢复一次，比对恢复前后的条数和附属数据。现在只验过「备份写得出来」。
- [ ] **④ 核心流程不用我在场也能走完**
      收藏 → 整理 → 找 → 同步，四步全程只看界面文案，不问人。
- [ ] **⑤ 装到一台干净浏览器上能跑起来**
      全新 profile、没有任何历史数据，装上就能用（现在从没验过空数据的样子）。

## 记录分两头

- **过去** → `CHANGELOG.md`：每一版做了什么、**为什么**、实测数字
- **未来** → 本文上面那五道门槛，勾一条划一条

⭐ 老徐要的「有记录」是这个意思：光看版本号往上蹦，看不出**离能发布还有多远**。
