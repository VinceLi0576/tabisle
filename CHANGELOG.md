# 更新记录

> 版本号怎么走、1.0 是什么 → 见 `版本规矩.md`

## v0.11.3 · 本地开发

**尺寸 token 第三步：间距。外加一个占地方的空框。**

- **间距 18 种 → 9 档**，135 处单值全部走变量。
  🔴 **原计划推翻了**：本来要按 4 的倍数收（6→8、10→12「往松走」），实采之后发现 **6（36 次）· 8（33 次）· 10（19 次）是三个各自的主力，合计 88 处**，而且用途分得很清——6 是标签内部、8 是工具栏、10 是侧栏行。并掉等于一次动 88 处、还抹平三个层级。⇒ 三个都保留。
  ⚠️ 多值间距（ 这种，316 处）这轮没动。
- 🔴 **「还没写说明」那个空框不再占地方**。原来没写说明时也渲染一个跟标题一样高的框，一屏重复六遍、吃掉将近一半竖向空间，而且**每一遍都没有信息量**——说明框上一版已经改成只读，那句「点右边那一条」根本点不动。
  实测：**一屏能看到的一级分组从 5 个变成 8 个**。

### 这三批收敛下来，最值钱的一条判据

**并档之前先看它们各自用在哪，🚫 别照数字分布盲并。** 三次都撞上了：

| 差 1～2 像素的两个值 | 实际是 |
|---|---|
| 字号 14 / 15 | 书签名 vs 文件夹标题 —— 并了之后**两者一样大，层级没了** |
| 圆角 8 / 10 | 按钮输入框等**元素** vs 卡片面板等**容器** |
| 间距 6 / 8 / 10 | 标签内部 vs 工具栏 vs 侧栏行 |

光看「21 种收成 5 种」这个数字，这三处都会被并掉。**收敛的对象是长尾，不是主力。**

> 版本号怎么走、1.0 是什么 → 见 `版本规矩.md`

## v0.11.2 · 本地开发

**尺寸 token 第二步：圆角和字重。**

- **圆角 17 种 → 6 档**，148 处单值全部走变量，一处写死都不剩。
  🔴 **8px 和 10px 没并** —— 实采了它们各自用在哪：8px 全是按钮、输入框、列表行这类**元素**，10px 全是卡片、面板这类**容器**，是两个层级。跟上一版 14px vs 15px 一个道理。设计线建议的「三级」方案会把这两个并掉。
  ⚠️ 还有 23 处多值圆角（ 这种），这轮没动。
- **字重 8 档 → 3 档**：400 / 600 / 700。
  🔴 主力是 **600**（40 次），不是设计线建议的 550。而 450 / 550 / 650 这三个非标准值多数字体渲染不出，**写了可能根本没生效** —— 一并删掉。
- 实测：主按钮 8px、卡片 10px、书签卡 8px；页面上的字重只剩 400/600/700 三种。

> 版本号怎么走、1.0 是什么 → 见 `版本规矩.md`（老徐 260915 问的）

## v0.11.1 · 本地开发

**尺寸 token 第一步：字号收敛。** 起因是设计线实采出来的一组数字——字号 21 种／圆角 17 种／字重 8 档，尺寸维度 932 处硬编码，`:root` 里定了 `--radius: 8px` 而全项目 165 处圆角只有 1 处引用它，**变量立了等于没立**。

- **新增 `tokens.css`**，四个入口页面（新标签页、侧栏、备份页、AI 设置页）都加载它——这是全站唯一一份尺寸档位表。
- **字号 21 种 → 8 档**，293 处全部走变量，一处写死都不剩。
- 🔴 **没照设计线给的「五级阶梯」做**：那个方案会把 13px（44 处，第二主力）并进 14px，动到的是正文主体。改成**保住主力、只收长尾**——12／13／11 三个值占了全部用量的 60%，各自成档。
- ⚠️ **第一版并档并出了问题，真机验出来的**：14 和 15 被并成一档之后，**书签名和文件夹标题变成一样大，层级没了**（原来 14 vs 15 是两级）。已把 14 挪回正文档。现在是域名 12 / 书签名 13 / 文件夹标题 15，三级清楚。
  ⭐ 判据：**并档不能把有层级关系的两个值并到一起** —— 光看数字分布看不出来，得看它们各自用在哪。

## v0.11.0 · 本地开发

**标签管理页**（老徐 260914 提的，一直欠着：「点『标签』其实应该跳到一个专门的标签页面，把每一个标签都定义一下，让我能在标签页里做统一的管理和整理……像链接的标签、文件夹的标签，全都得独立开来」）：

- 顶栏「标签」两个字**从说明文字变成入口**，点进去是专门的一页。
- **两栏并排**：左边书签的标签，右边文件夹的标签——现在它们本来就是两套互不相干的名单，这一页把这件事摆明了。
- 每个标签一行：**字形 · 名字 · 说明 · 用量 · 改 / 并 / 删**。
  - **用量**：书签标签数「有几条书签挂了它」（跟首页筛选看到的张数一致——标签按网址存，同一网址收藏两份算两条）；文件夹标签数「有几个一级分组打了它」。
  - **合并只有书签标签有**：文件夹标签就两个状态，没有合并的意义。
  - **「排到最后」那个标记只在文件夹标签上出现** —— 书签标签里可能留着历史数据的同名开关，不当真。
- **合并走两步点击**：点「并」那一行亮起来、其余可选行变虚线，再点一个就并过去；点空白处取消。🚫 **没用菜单做单选** —— 菜单的关闭只是把它藏起来，没有任何「关闭了」的信号可以等，拿它做单选会永远挂着。
- **删标签只去掉「谁挂了它」**，书签和文件夹本身一个不动，删之前会说清楚有多少条受影响。
- 跟分组整理页**互相让位**，不会同时开着。
- 🚫 **这一页不做批量打标签**（老徐 260914 明确废止）。书签的标签在书签详情侧栏点，文件夹的在分组详情侧栏点。

## v0.10.62 · 本地开发

**这一版来自 260915 多 AI 调研的核实官那一轮。四条待核声称里一条推翻、三条确认，但严重性跟报告给的差很多。**

### 🔴 一、被标「待定」沉到底部之后，上下移动的是他看不见的那份顺序

这是四条里**唯一真会让他白干**的。

首页把带「待定」的一级分组重排到最后 ⇒ 屏幕上的顺序 ≠ 书签栏里的真实顺序，而上下移一直按真实顺序算。核实官逐格复算出的现象：

- 沉在最底下的分组点「上移」，会说**「已经是第一个了」**（它在真实顺序里确实是第一个）
- 点别的分组「上移」，**屏幕上一格不动，书签栏里已经换了位置**
- 点某个分组「上移」，掉下去的是屏幕上完全没挨着的另一个

每一次点击都实打实写回了书签树，写的却是他看不见的那份顺序。**平时被「待定沉底」盖着看不出来，等他哪天取消待定标记，一级分组的排列就是乱的——那时候已经回溯不了是哪几次点的。**

修法：一级分组的上下移改成按屏幕顺序算。⚠️ **第一版改完真机一验，发现修得不完整**：待定的分组永远被排在最后那一段，它不可能「往上走出那一段」，结果变成「屏幕纹丝不动、书签栏里从第 1 跳到第 12」——比原来的错更难察觉。所以又加了一层：**上下移只在自己所属的那一段内部走**，到段边界就老实说到头了。实测修完之后真实顺序不再变。

本仓库此前**没有任何一条测试覆盖到「显示顺序」这一层**（核实官发现的），这次补上了。

### 🔴 二、把文件夹标签改名之后，每次打开页面都会被改回默认名

迁移逻辑靠「名字是不是叫『已上线／待整理』」判断是不是上一版的默认值——它分不清「这是旧默认值」和「用户自己就想叫这个」。中招时名字、字形、说明、颜色**四样一起**被盖。改成：只补缺的，不动已有的。

### 🔴 三、名单空着时侧栏那条路会直接报错

上一版修同步吞键时，我让「名单为空」直接抛错。核实官指出：**代码修了不等于数据回来了**——他的名单现在仍是空的，要等首页被打开一次才自愈，在那之前侧栏点「确定／待定」一直报错。现在侧栏这条路也能自己补回默认名单。

### ⬜ 推翻一条：整理页拖放并没有绕过防成环

agy 报的「能把父文件夹拖进自己的子文件夹」是**误报**。核实官逐条代入验证：防成环那道检查在拖放分支**之前**，而且整理页的块自己带着「我是文件夹」这个标记，所以那道闸门对它照样生效；三条落点全被挡住。`git blame` 也确认那段代码从 09-13 起一字未动，不是后来改过。**这条不用修。**

### 📌 核实官另外查出来、各家都没提的

- **他真实书签树里有 6 组同名文件夹，但一级分组零重名** ⇒ kimi 和 codex 报的「同名分组标签互串」当下不可能发生（标签只有一级分组才有）。但**颜色不受这个限制、任何层级都按夹名取** ⇒ 他的两个 `自媒体` 今天就是同一个颜色，动一个另一个跟着变。
- `inbox` 有 6 个同名 ⇒ 只要沾过一次旧式的按名锁，就**永远**共享且不会自愈（当前没有按名锁残留，没咬到人）。
- 他日常 Chrome 的附属数据里有一条**对不上的孤儿记录**：键是 `云claude`（无空格），而那个一级分组实际叫 `云 claude`（有空格）⇒ 那条颜色和状态在他真实树上不生效。

## v0.10.61 · 本地开发

**这一版全部来自 260915 多 AI 调研里那家「能真跑」的子 agent 实测出来的问题。五条，全是静默丢东西。**

### 🔴🔴 一、每同步一次，文件夹标签名单和 emoji 兜底库就被整键删掉一次

- **实测**：手动同步前 `meta` 有 7 个键、`folderTags=[确定,待定]`、`emojiRules` 862 字符；同步后只剩 5 个键，那两个**没了**，而且**删掉的版本被上传到云端** ⇒ 两台一起丢。备注、分组、锁一个没少 ⇒ 不是整体损坏，是精确地少了这两个。
- **病根**：同步三方合并最后拼结果时，`meta` 是一个**硬写死键名的对象字面量**——只列了备注、分组、标签、锁、说明五样。**凡是没列进去的 meta 子键，每同步一次就被删一次。** `emojiRules` 这条坑早就在（只是他没自定义过、没咬到他），`folderTags` 是今天新加的，一同步就丢。
- **修法**：把这两个补进去，数组型的（文件夹标签）按 id 合并、文本型的（emoji 库）谁改过用谁的。另外把「对面是不是旧版」那张单**拆开**——新键几乎所有现存快照都没有，混进去会让页面永远提示「有旧版」。
- **两道测试钉住**：一道查代码形态（`app.js` 里出现的 meta 子键，必须全在合并结果的字面量里，**发现新键当场报**），一道真跑合并（三边都有 / 只有本机有 / 新建 / 真删 / 文本谁改用谁 / 不影响旧版判定）。验过有效：撤掉修复，6 条立刻失败。

### 🔴 二、名单空着的时候，点「确定／待定」反而把这个夹已有的状态清掉

上面那条的连锁反应：名单被同步吞空之后，后台那段会把传来的 id 全过滤掉 ⇒ 直接删掉 `ftags`，**不报错**。而首页内存里还留着一份 ⇒ 首页照常显示「待定」，侧栏和存储里已经是空的，两边不一致。现在名单为空时**停下来说清楚**，不再顺手删。

### 🔴 三、侧栏改名 ⇒ 颜色和「确定／待定」当场丢；说明和锁不丢

颜色和文件夹标签按**夹名**存，说明和锁按**稳定身份码**存 ⇒ 同一次改名，一半东西没了一半还在。而且首页右键改名**本来就有搬家逻辑**，侧栏这条路一直没有——同一件事两条路两个结果。现在侧栏改名也搬。

### 🔴 四、首页右键「标记为废弃」完全失效，每点一次还往数据里塞一份垃圾

菜单写的是书签标签（`tags`），而判断读的是文件夹标签（`ftags`）⇒ 点了界面毫无变化，每点一次往 `tags` 里多塞一个没定义的 id，**而那个 id 会被继承给夹里每一条书签**。现在改成跟侧栏同一条路，菜单文字也跟着标签名走，并且只对一级分组出现。

### 🔴 五、那个「自己刚写过就忽略 1.2 秒」的窗口，两头都不对

- **实测**：浏览器的移动事件比写入的 promise **还早到**（1.2ms vs 1.3ms）⇒ 标记根本来不及设，自己那次照样触发一轮全量重画，**想省的那次没省掉**；
- 而标记设上之后的 1.2 秒里，**别人**（另一台机器／同步）的改动被整个吞掉，吞了就没人再排一次 ⇒ **页面一直显示旧树，不自愈**。实测：点完上移第 703 毫秒从外部加一条书签，页面 4 秒后仍显示旧条数。
- **改成按内容比对**：拉完数据比树的指纹，真变了才重画。自己挪完指纹一致 ⇒ 天然跳过；别人改的 ⇒ 照常重画。🚫 不再有时间窗口。
- 顺带修掉：挪一格之后「待定 · N 个」那条分隔条会瞬间跳到整页最上面（重排时只排了卡片、没管它）。

## v0.10.60 · 本地开发

🔴 **补三个漏掉的偏好，并把那条测试从「逐个点名」改成「自动发现」**：

- 工作台的三个偏好——看的是哪一页（`deckTab`）、整块折没折（`deckCollapsed`）、用哪个搜索引擎（`webEngine`）——进了首页的默认值表，但**没进备份名单**。症状是本机用着好好的，换台机器或恢复备份之后全部回到默认，而且不报错。
- 讽刺的是仓库里早有一条测试专门防这个坑，但它是**逐个点名**的（只查了 `pinnedNote`、`pinnedCollapsed`、`foldDefault`、`orgCols`），后来新增的键根本没人查。
- 现在改成：**默认值表里的每一个键，除了一张「故意只留本机」的白名单，都必须在备份名单里**。以后再加偏好，漏了当场报。验过有效：故意删掉一个键，测试立刻失败；还原就过。

## v0.10.59 · 本地开发

**「确定 / 待定」只给一级分组**（老徐 260915：「这个确定待定，是要只针对于根目录吗？我们只针对根目录操作」）：

- 子文件夹的详情侧栏里，那一整段直接不显示——颜色、说明照旧能改。
- 后台也拦一道：不是书签栏直接下级的文件夹，传过来的状态一律不收。
- 首页判断「谁该沉到下面那一组」时只看一级分组，子夹被标过也不再把整棵子树压淡。

实测：点一级分组「云 claude 与知识库」→ 那一段在；点二级子夹「inbox」→ 那一段没了，颜色九个格和说明框都还在。

## v0.10.58 · 本地开发

**侧栏里各段等宽了**（老徐 260915 截图指出第一段的框比下面几个宽）：

- 病根：`.edit-section` 本来就带框和外边距，我给新加的 `.fp-box` 又套了一层左右 `margin` ⇒ 新框比第一段窄 14 像素，看着就是"第一段更宽"。去掉那层多余的。
- **侧栏所有左右内边距归到一个变量**（`--panel-pad`，值 16，跟内容区的内边距一致）。顶上标签行、那排操作、每一段的框，实测左边全是 16。
- 第一段「文件夹」信息也统一成同一套框。

📌 顺带实测澄清一条：**首页的工作台和下面的一级文件夹外框是完全等宽的**（都是 280→1211，931 像素），不存在谁比谁宽。

## v0.10.57 · 本地开发

**换上老徐 260915 给的一套颜色标准**（八个，带名字和 RGB）：克莱因蓝 · 蒂芙尼蓝 · 普鲁士蓝 · 提香红 · 中国红 · 勃艮第红 · 申布伦黄 · 凡戴克棕。

- ⚠️ **蒂芙尼蓝那条他给的两个值差 1**：`#81D8CF` 算出来是 RGB(129,216,**207**)，他写的是 208。按 RGB 反推是 `#81D8D0`，也是这颜色的通行值 ⇒ 取 `#81D8D0`。测试里加了一条「每个 RGB 必须跟自己的十六进制对得上」，以后再抄错会当场报出来。
- 普鲁士蓝他备注「笔记目前无内容」⇒ 先用该颜料的通行值 `#003153`，等他给了再换。
- 🔴 旧颜色**没有自动替换**——那是他自己给各个夹选的，不该我替他改。调色板换成新八个，哪个夹想换点一下就行。

**文件夹状态标签改名「确定 / 待定」**（他 260915 的说法），确定＝克莱因蓝，待定＝灰。他没自己改过名字的会自动跟着换。

**侧栏整体重排**：

- **上移 / 下移 / 移出 / 收进 / 锁定 五段等分一排**，图标在上文字在下、居中（他原话：「像搜索、快捷方式、最近访问那样居中弄，跟上面的详情页对齐」）。**锁定从下面那一段并进了这一排**。左右边距跟顶上「详情 ｜ AI」对齐（都是 14 像素）。
- **每一段单独框起来**：这个分组定了没有 · 这个文件夹该放什么 · 颜色 · 子文件夹。
- **颜色一行一个**，色块＋名字＋编码排开（他原话：「颜色上下两行 不用这么挤的」——两列会把「克莱因蓝」折成上下两行）。**点编码直接复制**。
- **「里面有什么」那张清单去掉了**（他原话：「不太需要了，名字也不太好」），只留「＋ 在这里面新建一个」。
- 侧栏设计基准定 **380 像素**（Chrome 侧边栏实际宽度由用户拖，扩展改不了，只能让内容按基准排版并自适应）。

🔴 **一个静默覆盖的坑**：`editor.css` 比 `panel.css` 后加载，它里面那份旧的 26×26 小色块样式把新的颜色格子整个压掉了——实测每格只剩 26 像素宽，而肉眼只会觉得"怎么还是老样子"。同理 `.page-head` 的内边距也写死在那边，改成用变量才对齐得上。

## v0.10.56 · 本地开发

**文件夹有了自己一套标签，跟书签那套完全分开**（老徐 260915：「我刚才讲的是文件夹整理，不是标签组。标签组是标签组，文件夹自己也要有标签组。比如这个是『已经上线』的，我们总共有 7 个，其他都是『待整理』」；两套还是一套由他拍的「完全两套，互不相干」）：

- **两份名单各存各的**：书签标签在 `tags`，文件夹标签在 `folderTags`。顶栏那一排标签只列书签的，文件夹的只在文件夹详情里出现。
- **默认给两个**：「已上线」（蓝）和「待整理」（灰，带「排到最后」开关）。
- **在侧栏里点**：打开任意文件夹的详情，颜色下面就是「这个分组的状态」，点一下切换。
- **老标记会搬过来**：之前是拿书签标签（名叫「废弃」）标文件夹的，升级时自动搬到文件夹标签上，标记不会凭空消失。
- 🔴 **同步那一段两套一起走**：`tags` 和 `folderTags` 用同一套「按 id 合并、顺序跟着动过的一方」的规矩跑两遍。漏掉哪一套，另一台刚建的标签就会被整份覆盖掉——这正是上周出事故的那块。

实测全链路：侧栏里点「待整理」→ 首页立刻出现分界线「待整理 · 1 个」、那个夹变灰、左栏同步分组；再点一次撤掉，分界线消失、存储里干净；书签树 848 条 189 夹全程不变。

## v0.10.55 · 本地开发

**二级文件夹也有自己那条右边竖条了**（老徐 260915：「二级文件夹……其实也需要有这个箭头出现，二级这一列在右边显示这个颜色」）：

- 上段收起展开、下段开详情，跟一级同一套手感。
- 窄一档（28 像素，一级是 38），颜色用它自己那一层的层级色，一眼分得出是第几层。
- 侧栏在讲它的时候，这条也会亮。

实测：163 条二级竖条，条高跟二级框高一致（贯通到底）、贴右边、卡片不会压到它上面；点上段收起展开正常且左边那颗三角同步转；点下段开详情并高亮，同一时刻只亮一条。

## v0.10.54 · 本地开发

**文件夹分成「正式」和「待整理」上下两组**（老徐 260915：「我只用 7 个文件夹，这些属于正式的……其他的文件夹都是灰色的待整理，相当给文件夹多了个分组。侧边栏就会分上下 2 组，页面就这么显示」）：

- **没打「待整理」标签的就是正式的** —— 默认状态，不用给那 7 个正式的逐个打标签，只标要沉下去的那些。
- **首页两组中间横一条**，上面写「待整理 · N 个」，一眼看出分界线在哪、下面有几个。
- **左栏也分上下两组**，同一条分界。
- **下面那一组整体压淡一档**（鼠标移上去恢复正常），就是他要的「其他的文件夹都是灰色的」。
- 分界线上的名字跟着标签走 —— 标签叫「待整理」就写「待整理」，改成别的名字也跟着变。

实测：给最后 3 个一级夹打上标签 → 分界线出现、写「待整理 · 3 个」，线上面 23 个全不灰、线下面 3 个全灰，左栏同步分组；撤掉标签后分界线消失、灰色还原，书签树 848 条 189 夹不变。

## v0.10.53 · 本地开发

- **标题栏那颗「说明」按钮和它那套就地编辑框整套去掉了**（老徐 260915：「这去掉，我们从侧边栏展开去填写就行了」）。说明现在只剩一条路：点右边那整条 → 在侧栏里填。连带「让 AI 看着写一条」那颗也随之退场（同样去侧栏）。说明框本身照旧显示，只是不接编辑。
- **侧栏在讲谁，谁右边那条竖条就亮着**（他原话：「侧边栏显示那个，就高亮我标的 1 或者 2 这文件夹或者文件」）。文件夹和书签各自那条都会亮，而且互斥——开着侧栏一眼知道它说的是哪一个。分组整理页里那一块也跟着高亮。
- **侧栏顶上「详情 ｜ AI」改成左右两段铺满**（他原话：「详情跟 AI 顶部就跟搜索、快捷方式这样的左右显示好看」），跟首页工作台那一排同一个做法：各占一半、各自一个颜色（详情蓝、AI 紫）、底下一条粗线标当前在哪一页。实测各 159 像素、切换正常。

## v0.10.52 · 本地开发

- **同步按钮直接摆在状态条旁边**（老徐 260915：「我改了，马上就能点同步；再改了，我自己还能点」）。原来要「先点云端最新版 → 进备份页 → 再点同步」三步，现在顶栏「云端最新版」右边就是一颗「⟳ 同步」。
- **分组整理页的方向键改成游戏手柄的十字**（他原话：「把它变成像游戏手柄一样的上下左右」）：上下＝在同一个爹里往前往后排，左＝移出去升一层，右＝收进上面那个夹。方向本身就是含义，不用再看四个格子猜。
- **那个灰点撤了**（他原话：「现在那个很灰的点我不太喜欢，不好看」）——没有子夹时原来占位画一个 `·`，现在留空。
- 🔴 **「排到最后＋显示成灰」不再靠标签名字硬匹配**。原来代码里写死认「废弃」两个字，他一把标签改名成「待整理」，排序和变灰就**静默失效**。现在改成标签自己带一个开关（编辑标签时勾「打了这个标签的排到最后」），叫什么名字都不影响；老数据里名叫「废弃」的会自动补上这个开关。徽章上的字也跟着标签名走。
  实测：给「GPT」打上这个标签并把标签改名成「待整理」→ 它从第 1 位跳到第 25 位（最后），徽章写着「待整理」，首页和分组整理页一致；撤掉标签后回到第 1 位。

## v0.10.51 · 本地开发

**分组整理页**（老徐 260915：「一个灰色箭头拖到这里放进文件夹，我就有点不太懂了。按理说我应该直接叠到别的文件夹上面」「既然能往上移一移、往下移一移，那左右呢？」）：

- **拖放落点改成三段**：拖到一块的**上边缘**＝排它前面 · **下边缘**＝排它后面 · **中间一大片**＝放进那个文件夹。跟所有文件管理器一个手感。原来那个又窄又得先看懂的 `↳` 小图标**整个删掉**。压在中间时整块高亮，一眼看得出会放进哪。实测：上边缘 `drop-before` · 中间 `drop-into` · 下边缘 `drop-after` · 压自己身上不给放（防止把夹放进自己）。
- **每块加四个方向键** ▲▼⇤⇥。类名和属性跟首页那套完全一样，所以**刚做好的「按住方向键＋滚轮连着挪」在这一页直接就能用**。
- **这一页挪位置也不跳了**：挪完把滚动补偿回去，实测那块在屏幕上位移 **0 像素**。
- **「没写说明」那四个字不再挂在每一块上** —— 它在 163 个块上重复，占地方还没信息量。

**首页右边那条竖条分成两段**（老徐 260915：「要在左边找一个小箭头去展开缩小，体验太差了」）：

- **上段＝收起／展开**（跟左边那颗三角同一件事，状态一起画），**下段＝这个夹的详情**。收起展开这件事不用再把手移到左边去。
- 「捷」按钮**从右上角挪到右下角**：右上那一行是标签徽章（官/自/备…），放上面会叠在一起，而且卡片是裁切的，贴着竖条会被切掉半颗（他截图指出来的就是这个）。

**两个实撞记一笔**：

- 整理页的点击处理器一进来就 `stopPropagation`，导致 document 上那条方向键委托在这一页**根本收不到**，按钮点了毫无反应。方向键必须在那个处理器里自己接一次。
- 竖条下段的 `flex: 1` 被上面 `.fstrip > button` 的 `flex: none` 按特异性压掉了，下段只剩 17 像素高，整条下面 600 像素点不到。选择器得写成 `.fstrip > .fs-more`。

## v0.10.50 · 本地开发

**右边一整条＝这个文件夹的入口**（老徐 260915：「右边这个箭头……放在整个框的最右边，可以做宽一点。相当于整个文件夹，甚至展开子文件夹时，右边一整条都属于关于整个文件夹的定位」）：

- 标题栏里那颗「›」挪到框的最右边，变成**贯穿整个框的一条竖条**（38 像素宽）。折叠时是一小条，展开时一直到底，点哪儿都是开这个夹的详情侧栏。实测 26 个夹都有，条高跟夹高一致，卡片不会压到它上面。
- **说明框只显示、不再接编辑**（他原话：「点每一个文件夹上面的备注名，它就直接跳出一个框……其实这些都不需要，下面这一段不能直接操作」）。要改说明走右边那一条进侧栏。没写说明时那行字改成指路。
- 说明框里不再重复报条数——标题栏右边已经有一份。

**上下移重做**（他原话：「我一点，整个页面都在刷新、跳动和滚动……能不能做成像鼠标滚轮那样直接平滑地滑上去」）：

先量了旧做法一次点击的代价：**整棵树全量重画 2 次（23920 个节点 ×2）、耗时 1.5～1.8 秒、页面被拽着跳 125～186 像素**。三个病根——① `render()` 是全量的 ② nudge 自己刷一次、浏览器的书签事件又刷一次 ③ 挪完 `scrollIntoView` 强行把那个夹拽到视口顶部。

- 一级夹同级上下移改成**只换两个 DOM 节点的位置**，配 FLIP 滑动动画（两块互相错身滑过去，220 毫秒）。**页面一步都不滚**。
- **按住方向键 ＋ 滚轮连着挪**（他原话：「点住，然后用鼠标滚轮往上滚一层、往下滚一层」）。两种方式都在：点一下走一格，按住滚轮连着走。按住 ▲▼ 滚的是同级顺序，按住 ⇤⇥ 滚的是层级深浅。松手那下的点击不会多走一格。
- 自己刚写完的那次，浏览器回来的书签事件不再重画一遍。
- 实测：按住滚轮连挪 5 格，**全量重画 0 次、页面跳动 0 像素**。单击那条路还剩一次多余重画，下一轮收掉。

## v0.10.49 · 本地开发

- **搜索引擎换成老徐点名的八个**：百度 · 必应 · 谷歌 · 搜狗 · B站 · YouTube · GitHub · Twitter。知乎、小红书、抖音他说先不要。
- **名单改动后会回落**：存着的引擎要是已经删掉了，自动回到第一个（实测：偏好里留着已删的「知乎」，打开后亮的是百度、提示语也跟着对）。否则一个都不亮，搜出来还不知道用的哪家。
- **四段前面各加一个 emoji**（老徐 260915「不要太夸张的那种」）：🔍 搜索 · 📌 快捷方式 · 📥 收集箱 · 🕘 最近访问。折起来也在。
- **搜索那页不再显示「紧凑／详细」**（他原话：「搜索这里没有所谓的紧凑型嘛，有吗？就这一个比较特殊嘛」）——那页一张卡片都没有。另外三页照旧能切。

## v0.10.48 · 本地开发

工作台那四段按老徐 260915 的要求重排、上色、排到头：

- **顺序改成**：搜索（最左）· 快捷方式 · 收集箱 · 最近访问。默认停在第一页。
- **四段各一个颜色**（蓝 / 紫 / 橙 / 绿），**等分铺满一整行**，不再缩在左边一小截。实测四段各 314 像素、标签行 1274 / 整块 1401。折起来颜色还在，折着也一眼看出是哪四块。
- **搜索引擎从 3 个加到 8 个**：百度 · 必应 · 谷歌 · 搜狗 · DuckDuckGo · 知乎 · B站 · GitHub。这一排也铺满整行（各 171 像素，不横向溢出）。
- 病根记一笔：标题栏里原有一个 `hd-toggle` 弹性占位，它会跟标签行**平分**剩余空间（实测各 634），怎么调标签自己的宽度都排不到头。删掉它才铺满。

## v0.10.47 · 本地开发

顶上「最近访问」「快捷方式」「收集箱」三块合成**一个带横排标签的工作台**，外加一个能换引擎的搜网页框（老徐 260915：「这三个组显示下来占的地方太多了，在顶部直接长期占用了三栏……做成类似浏览器书签页那样，在这个区域顶部横排一列标签」）：

- **四页横排**：最近访问 · 快捷方式 · 收集箱 · 搜网页。点标签切页，标签上带条数，一眼知道各有多少。
- **整块能折**。折起来就剩一行摘要「最近访问 8 · 快捷方式 0 · 收集箱 13 · 搜网页」，实测从 257 像素缩到 61 像素。**折着的时候点标签＝展开并切到那一页**，不用先展开再点。
- **收集箱从下面那一排收上来了**。它原来那三颗上移／下移／复位按钮没有落脚点，一起退场（位置偏好留着，将来要退回旧排法还在）。
- **新增「快捷方式」这一页**：打了「捷 快捷方式」标签的网址都排到这儿，一组最多 16 个，多出来的行尾给一个「还有 N 个」，点它就在下面只看这个标签。
- **钉的入口在卡片上**（老徐 260915：「我点了地址就是快捷方式来」）：鼠标移到任意一条书签上，点右上角那颗「捷」就钉上，再点一次取消。**取消只去掉标签，书签还在原来的文件夹里**。钉过的那颗一直亮着，一眼看出哪些已经钉了。钉完自动切到快捷方式那一页。
- **搜网页**：百度 / 必应 / 谷歌一键切，记住上次用的那个。地址栏只认默认引擎，想临时换一个得进设置——这一格补的就是那件事。不要任何新权限，只是拼个网址。
- 右边那条竖条从 22 像素加宽到 30，文件夹标题栏那颗「›」从 26 加到 34（老徐 260915：「右边的箭头这不够宽一点」）。

顺手修掉两个：

- 运行错误里长期挂着的 `Cannot read properties of null (reading 'dataset')`——点非文件夹那些块的三角时，事件委托拿到的是 null。清空历史错误后把四页点一遍、折展、开关整理页、搜索走一圈，运行错误 0 条。
- `foldDefault`、`orgCols` 两个偏好一直没进备份名单，换台机器读不回来，补上了。

## v0.10.46 · 本地开发

「整理文件夹」改名「**分组整理**」，顶栏那颗「＋ 新分组」并进去了（老徐 260914：「既然叫分组整理，那我要创建分组，直接在这个文件夹里创建不是更好吗？要不然在外面建个文件夹再拉到底下，来回拖拽太麻烦了」）：

- **每一层最后都有一个虚线格「＋ 在这儿新建文件夹」**，点它就在那个位置建，不用先在外面建好再拖进来。一级、二级、更深层都有。
- **子文件夹改成平铺**，右上角能选每行摆 2 / 3 / 4 个。一级还是一行一个。
- **详情侧栏多了「＋ 在这里面新建子文件夹」**，点开哪个夹就在哪个夹里建。
- 页面顶上那行说明改写成「这一页直接改浏览器书签栏里的文件夹结构，改完立刻生效、也会同步到别的电脑」，并说清楚每一层末尾那个虚线格是干什么的。
- 新建的文件夹会打「这是我有意建的」记号，免得十分钟内被同步当成回声删掉。

## v0.10.45 · 本地开发

- 整理页里点任何一块（一级二级都一样），右边侧栏直接开这个文件夹的详情——名字、颜色、说明、锁、挪位置都在里面。**不再跳回首页去找那个夹**（老徐 260914「跳来跳去、不知道跳到哪里去」）。正在看的那一块会高亮。
- 三角开合、全部展开/折起照旧，不受影响。

## v0.10.44 · 本地开发

折叠这一排挪到标签那一行的最右边，改成看得见的按钮（老徐 260914「折叠和展开的操作放在右边比较方便」）：

- **默认 自定 / 折叠 / 展开**：没单独设过的文件夹，打开页面时按这个来。
- **全部折叠 / 全部展开**：这一次全折或全开，会记住。
- **回到默认**：把逐个设过的那些忘掉，交回上面选的默认。
- 单独设过的夹优先于全局默认；改了默认之后如果没看出变化，是因为还有逐个设过的在压着，提示会说明，点「回到默认」让它们也跟着走。
- 工具条改成可以换行，塞进这一排之后按钮里的字不会再被折成两行。

## v0.10.43 · 本地开发

「整理文件夹」这一页重做（老徐 260914：「太密了……不要一级二级给它拆出来，就是那种缩线的、类似于 Markdown 一样缩进这种状态」）：

- **改成一棵缩进的树**。子文件夹往右缩一层，用一条竖线连着，最深到四级。原来「一级排一排、二级排一排、底下再来一段」的拆法没有了。
- **块放大了**：从一行小条变成两行——名字，以及条数、连子夹一共多少条、几个子夹、说明、锁。按层级分四档高度和宽度，不再一刀切。
- **底部「还没有子文件夹的组」那一段去掉了**，改成每一行自己标：有子夹的给展开三角，没有的标「无子夹」；顶上一行汇总「26 个 · 15 个有子文件夹 · 11 个还没有」。
- **拖拽改成上下**：落点按上下中线判，提示线也从左右竖条改成上下横条。多了「全部展开 / 全部折起」。
- 🔴 顺带修一个老毛病：整理页开着的时候拖一次，「最近访问」会被重新放出来盖在上面。这条在这次改动之前就有。
- 新增 13 项测试，共 260 项通过。

## v0.10.42 · 本地开发

- **文件夹颜色改成 8 个带名字的固定色**：蓝、绿、黄、红、紫、青、橙、灰，外加一个「不上色」。改颜色的入口从首页那根细色条挪到**详情侧栏**（点标题栏最右边的箭头进去），那根色条太细，点不着。
- **顶栏「紧凑 / 详细 / 列表」右边多了「⇕ 折叠」**：一次全部折叠、一次全部展开；另外能定默认状态——我自己逐个设（原样）、一律折叠、一律展开。自己单独设过的那个夹优先，不受全局默认影响。
- **点左侧栏的文件夹不再弹出 AI 面板**，也不再往对话里记一条。范围照样设上，看左栏那个「AI 范围」小标就行。

## v0.10.41 · 本地开发

- **点标题栏空白处＝展开收起**，跟每个文件夹一模一样；🚫 不再弹侧栏。侧栏还是走标题栏最右边那个箭头按钮。
- **最近访问和收集箱的标题结构改成跟下面的文件夹一样**：同一个展开箭头、同一条层级色带、同一个「1级」标记，只有颜色不同。
- **一级文件夹的标题栏上不再挂子文件夹快捷和标签筛选**，那些东西本来就在下面排着。标题栏现在只剩：箭头、名字、层级、条数、右边几个按钮。
- **说明框折叠展开都在**：写了说明就显示说明；没写就显示「几个文件夹 · 几条书签 · 点这里写一句」，点一下就能写。
- 🔴 修一个自己挖的坑：空说明那一行原来叫 ，撞上全局的 ，把框从 38 像素撑到 116 像素。改名解决。

## v0.10.40 · 本地开发

- **说明框两边往里收了**：左边跟标题文字对齐，右边留出跟标题栏一样的边距，不再顶到虚线上。
- 🔴 **收集箱和最近访问那两句不再写死在代码里**（老徐 260914「不能直接写到代码里面去」）。代码里只留一句默认文案；你自己改了就用你的，清空了就真的空着。这两块不是普通文件夹（一个根本不是夹，一个是书签栏根目录拿不到稳定标识），所以说明存在本机设置里，会跟着完整备份走。
- **折叠状态下点那一行的空白处，右边侧栏直接弹出来**，里面能看到这个夹有几条书签、几个子文件夹、说明和锁定开关。点名字改名、点按钮各管各的，不受影响。

## v0.10.39 · 本地开发

- 最近访问也有了自己的说明框；它标题栏后面补了一个撑开的空档，「紧凑」不再贴在标题旁边、右边按钮排齐了。
- 说明字号从 12 加到 13。
- 收集箱和普通分组标题栏右边加了留白，排序那几颗按钮不再顶到虚线上。

## v0.10.38 · 本地开发

接着上一版调整说明的位置（老徐 260914 当面定的）：

- **分组说明从标题那一行挪到标题栏底下，自己一个小框。** 折叠时才出现，展开之后收起来，标题栏下面直接是书签。太长的说明最多显示三行。
- **没写说明的文件夹不出这个框**，折叠就是干干净净一行。想写说明：展开后点标题栏上的「说明」，或者走文件夹详情。
- **收集箱多了一句写死的说明框**（「在别处点星号收藏、没归类的网址都落在这里」），折叠展开都在。原来挤在标题行上的那句灰字去掉了。
- **最近访问从 16 条减到 8 条**，标题行上那句灰字也去掉，不写说明。
- 新增 2 项测试，共 238 项通过。

## v0.10.37 · 本地开发

一级文件夹统一成虚线模块（老徐 260914 定的规则：**折叠时这一行回答「这个文件夹是干什么的」，展开时内容本身回答「里面有什么」**）：

- **每个一级文件夹现在是一个完整的虚线框**，折叠和展开都在同一个框里 —— 折叠是一行摘要，展开是标题栏加内容区。边框用各自的分组色、很淡，三十多个分组摆在一起不抢眼；「最近访问」和「收集箱」保持原来那种明显的样式。
- **折叠那一行分两段**：左边是展开箭头、名字、层级、条数；右边是这个夹的说明，只显示一行，太长就省略，鼠标停上去看全文。
- **展开时不再显示说明文字**。原来「云 claude 与知识库」「伙伴云」那种长说明会悬在标题下面把版面撑散，现在标题栏下面直接就是书签。
- 说明改成点那一行文字就能编辑；没写说明的夹，鼠标移上去会出现「＋ 这个夹是干什么的」。折叠状态下点它会自动展开再打开编辑框。
- 二级、三级文件夹这一轮没动。
- 新增 7 项测试，共 236 项通过。

## v0.10.36 · 本地开发

接着上一版，这一版修的是核实官逐条反查之后确认成立的那几条 —— 全是「连续整理几小时」才会撞上的：

- 🔴 **单份备份太大时，会把本机已有的历史备份全部清空。** 实测：4 份加起来才 2 MB 的保险丝，被一份 6 MB 的新快照连坐清零，而界面上只说「这一份没保存」，不提还丢了别的。病根是多写了一行「把备份列表清空」——不写这一行本来就不会撑爆。已改成只记错误，历史一份不动。
- 🔴 **每开一个新标签页，每夹的「紧凑/详细」和收集箱位置就回默认。** 上一版以为只是没进备份，核实官查出更狠：这两项压根读不回来（读取时要列出键名，而它们没被列进去），所以是天天在丢，不是恢复备份才丢。已修，真机验过：改完另开一页还在。
- 🔴 **删掉一条、十分钟内在详情页把同一网址加回同一个夹，下一轮自动同步会静默再删一次。** 首页新建、恢复、撤销三条路都打了「这是我有意加的」记号，只有详情页新建漏了。已补。
- 🔴 **云端抖一下（比如 503），自动同步就再也不跑了。** 「做到一半」这个标记只有三处会清，全都碰不到这种情况，而每分钟那次检查见到它就直接返回 —— 上一版修的退避重试一次都跑不到。现在：服务器明确拒收（云端一个字没写）当场撤标记；标记搁过 10 分钟还在，就按「两边都保留」自己往下走，不会拿云端抹掉本机。
- **连着删重复超过 5 次，「一条都没删之前」那一版会被挤掉。** 详情页的两处删除以前各自拍快照、绕开了首页那条「90 秒内算同一批」的规矩。三条路现在共用同一个窗口。
- **把网址改成另一条书签已经在用的地址时，会静默盖掉那一条的显示名和说明。** 已改成已有的优先。
- 新增 12 项回归测试，共 229 项通过。

## v0.10.35 · 本地开发

260914 派外部 AI ＋ 子 agent 调研「连续几小时手工整理会撞上什么」，这一版修掉界面和本机这半边：

- 🔴 **首页每动一下就卡一秒多，现在不卡了。** 病根不在条数，在图标：每次重绘都给 700 多个图标各建一张画布去比对「是不是灰地球」，实测每个 3.11 毫秒、合计 2.2 秒，而且这活在重绘返回之后还在烧。现在按图标地址记住答案，第二次起直接查表。**实测主线程被占从 1413 毫秒降到 258 毫秒，最长一次卡顿从 1354 毫秒降到 237 毫秒。**
- **在侧栏写说明时，每敲一个字首页都整页重绘一次** —— 现在停手 400 毫秒才落一次；离开输入框、保存、关侧栏、关页面都会立刻把最后一笔冲出去。
- 🔴 **侧栏写失败一次就废掉**：之前只要有一次写入失败（后台睡着了之类），侧栏之后点哪条书签都只显示空状态，只能关掉重开。已修。
- 🔴 **「紧凑/详细」的每夹设置和收集箱位置没进备份** —— 恢复一次备份或换台机器就回默认，而同一次恢复又会把别的显示设置改成备份里的值，看着像随机丢设置。已补进备份。
- 🔴 **锁定的文件夹，右键「归入」和直接拖动两条路都能塞东西进去**，一声不吭。现在两条路都认锁。
- **拖动挪位置现在也有「撤销」**（以前只有菜单里的「归入」有），而且撤销前会复核落点：这几秒里原文件夹被动过的话，放回那个夹的末尾并说明。
- 「放回收集箱」的撤销按钮以前 0.3 秒就被「收集箱多了一条」顶掉，现在自己挪进去的不再重复提醒。
- 新增 7 项回归测试，共 223 项通过。

## v0.10.34 · 本地开发

- 🔴 **修：详情页的「详细说明」写了等于没写。** 侧栏每敲一个字都会发给后台，但后台的字段白名单里漏了它，收到就丢；点保存时写回的还是载入时的空值 ⇒ 这个框从上线起就没存住过任何内容，而且不报错。实测写一句话、读回存储确认已落盘。（260914 派外部 AI 调研底座时，codex 和 grok 各自独立指出同一处）
- 新增 3 项回归测试，共 216 项通过。

## v0.10.33 · 本地开发

- 收集箱头上多了三颗一直露着的按钮：▲ 上移一格、▼ 下移一格、⇱ 复位回最顶上。改的只是它在首页排第几（左侧栏跟着动），这台浏览器自己记，不动书签本身（老徐 260914「放到哪个地方我自己知道就行」）。

## v0.10.32 · 本地开发

- 收集箱也能折叠了，跟别的组一样点标题左边的小三角，折叠状态记住（老徐 260914「收件箱也可以折叠嘛」）。

## v0.10.31 · 本地开发

- **「最近访问」不再是一排小条**（老徐 260914「明确不要列表」）：跟收集箱一样用卡片。收藏过的就是那条书签本身，能进详情、能挪；没收藏过的是影子卡，只能点开。
- **每个文件夹标题栏上多了「紧凑 / 详细」**（老徐 260914「是否可以在每个文件夹这个位置点击展开」）：点一下只切这一组；顶栏那组还是管全部的，点它会把各组自己的选择清掉。

## v0.10.30 · 本地开发

- 「最近访问」换成跟收集箱同一套框：虚线框、蓝色标题、右边计数（老徐 260914「一模一样，用收集箱这种框架比较好看」）。

## v0.10.29 · 本地开发

- **新增书签自动填，不靠 AI**（老徐 260914「书签名这些下面的信息全都要手动填」）：详情页网址框下面两颗按钮——「从打开的标签页选」列出这个窗口最近打开的网页，点一条网址、书签名一起填好；「按网址取标题和简介」去读网页源码里的标题和它自带的一句话简介（第一次要同意一次「读取网页」权限，以后不问）。粘一个网址进新书签，同意过权限的直接自动取，没同意过的提示一句。GBK 网页也能正确读出中文。
- **根目录就是收集箱**（老徐 260914「根目录其实不需要放具体的网址……专门作为一个 Inbox」）：原来的「未分组」改叫「收集箱」，钉在首页最上面、虚线框，左侧栏也排第一。里面每条都有「归入…」按钮，点开列出所有文件夹（含二级），一键挪走、可撤销；右键菜单里所有书签也多了「归入文件夹…」。
- 在别的页面点星号存进根目录，首页会提示「收集箱多了一条」，每条只提醒一次。
- 清单里申请了「读取标签页」权限，用来列打开的标签页。它的提示文案跟已有的「浏览历史」是同一句，Chrome 不会再弹新提示。
- 新增 4 项测试，共 213 项通过。

## v0.10.28 · 本地开发

- **备份页重排（老徐 260914 四条）**：「立即同步 / 立即备份」搬到左侧「同步中心」顶上，随页滚动一直在；下面一行小字是当前同步状态。六段按日常用到的频率重排：多设备同步排第一，备份方案这种一次性的设置排到后面。
- **不展开也能点**：多设备同步、备份状态、版本与恢复三段的标题栏上直接放了「立即同步」「立即备份 · 下载」「刷新」，点它不会展开那一段。
- **备份放在哪**写明白了：本机版本存在浏览器分给插件的存储区（不是磁盘文件，要文件点下载）；云端快照的目录和同步最新版的文件路径按你的实际配置显示。
- 多设备同步开头加了「这台浏览器 / 云端最新版来自哪台」。
- 「先分清三件事」收成一条可展开的说明，不再占页首。

## v0.10.27 · 本地开发

- **卡片分两档**：顶栏「卡片 / 列表」变成「**紧凑 / 详细 / 列表**」。紧凑还是两行，但文字区放宽、字号加一号；**详细**多露三样——书签名原样（有显示名时才露，灰字，让你知道它跟显示名不是一回事）、详细说明的前两行、以及**最近打开时间**。列表这轮没动。
- 「最近打开」走浏览历史，对所有打开方式都算（浏览器自带的那个字段只在从它的书签栏点开时才记，从首页点开一律是零）。**没打开过的用橙色标出来**——判断一条要不要留，这是第一依据。
- 详情页顶上加了一条摘要（显示名 · 一句话说明 · 所在夹），跟详细版卡片是同一张脸，点进去不会换一套排版。
- 详情页多了「不要显示名了，就用书签名」——原来只有反方向的「用备注名作为书签名」。
- 新增 5 项测试，共 209 项通过。

## v0.10.26 · 本地开发

- **顶栏那颗同步药丸，点一下就进「同步与备份」页**。以前点它是"再检查一次云端"，你想进设置页却进不去。现在药丸只负责显示状态，同步本身每分钟自动跑，不需要人点；状态文案也从"点一下同步"改成"稍后自动跟上"。
- **「同步与备份」页顶上加了一段说明，把三件事分清**：同步是让几台浏览器一致（每分钟自动）；备份是留住历史（每小时归档到云端，本机只留几份保险丝）；恢复是回到某一份备份。
- 说明里还写了两件最容易搞混的事：**换一台新浏览器怎么开始**（连同一个账号目录 → 手点一次「立即同步」，第一次是合并不是覆盖 → 之后不用管），以及**两台来回切换要注意什么**（不需要"先下载、改完再上传"；唯一要避免的是同一分钟在两台上改同一条的同一个字段）。
- 新增 1 项测试，共 204 项通过。

## v0.10.25 · 本地开发

- **点文件夹也能弹出详情页了**。首页每个文件夹标题栏右侧多了一个「›」，点它（或右键菜单「文件夹详情…」）侧栏就显示这个文件夹的详情：改名、写「这里该放什么」、锁定/解锁、上移下移移出收进，都在一处操作；底下列出它里面有什么，点哪一项就跳到那一项的详情。
- 文件夹说明和锁定按稳定标识落库，只改这一个夹的那几项；解锁时会连老版本按名字记的锁一起清掉，不会「解了还锁着」。
- **修掉「删错了想恢复、恢复不回来」的根因。** 自动同步原来把所有冲突一律选「云端」——连「这条是旧同步回流、还是重新收藏」这种冲突也选云端＝移除。于是你删过的东西只要在三天内加回来（从备份恢复、撤销、再收藏一次），下一分钟就被静默删掉，而且另一台设备看到它也当回声再删一次。现在分三条判：
  - 从云端来的重新出现，一律保留——账号同步的回声在上传前就会被本机处理掉，到不了云端，所以云端来的一定是别的设备有意加的；
  - 本机由插件自己建的（恢复、撤销、挪位置、AI 执行、详情页新建），一律保留；
  - 本机外来的（浏览器账号同步塞回来的）：删除记号还新鲜（十分钟内）就当回声跟着删，过了就当重新收藏保留。
  两台模拟浏览器复现过完整链路：删 → 同步 → 加回 → 同步 → 两台都在。真机上今天下午消失了三次的测试文件夹，这一版稳稳留住了。
- 新增 10 项测试，共 203 项通过。其中一条盯住开发时撞到的死锁形状（侧栏刷新链自己等自己）。

## v0.10.24 · 本地开发

- **本机备份改成按用途分档保留**，不再按总份数一刀切。实测原来 20 个坑位里 16 份是「同步前」的（每分钟同步一次、每次拍一份），跨度只有 1.8 小时，真正救命的 4 份「删除前」正被它们往外挤，而且连「回到昨天」都做不到。现在：
  - **保险丝**（删除前、恢复前、手动、AI 执行前）留最近 5 份——本地备份唯一不可替代的价值就是"刚才那一下点错了能撤回"
  - **每天一份留底**，留 3 天——补上"回到昨天"
  - **同步前、定时**各只留最新 1 份——它们只对"这一次"有用
  - 历史版本那一层由云端负责（每小时归档，坚果云自己保留一个月）
- 实测：备份 20 份 → 8 份，插件占用的浏览器空间 5.7 MB → 2.5 MB，存一份备份从 3.6 秒降到 0.7 秒（原来每分钟后台都要搬一次 5 MB）。10 MB 上限的问题随之消失，不用换存储引擎。
- AI 执行一批改动之前也会自动留一份备份：撤销那套逆操作日志只活在内存里、刷新就没了，这份是它之外的第二道保险。
- 备份页里"最多保留 20 份"那句话改成了实话。
- 新增 5 项测试，共 193 项通过；四条只数"备份累积到几份"的老测试改成看"到点有没有出新的一版"。

## v0.10.23 · 本地开发

- **重新收藏一个以前删过的网址，不会再被当成「旧同步回流」拦下来**。同步用「墓碑」记住删过什么，防止已删的东西在下一轮同步里又冒出来；但这个记号原来是永久的，而且按「路径＋网址」认——于是你几个月后重新收藏同一个网址，它还会跳出来问一次。现在记号三天后失效，超过九天自动清理。
  - 老版本留下的、没有时间记录的记号仍然照旧拦——那条保护本身是对的，不能顺手去掉。
- 新增 5 项测试，共 188 项通过。

## v0.10.22 · 本地开发

- **两个页面同时开着改备注，不会再互相覆盖了**。以前首页、侧栏、详情编辑器各拿着一份附属数据的快照，谁后保存谁把对方的整个盖掉——你在首页给一条书签加标签的同时在侧栏写说明，其中一样会悄悄消失，而且两边都显示成功。现在改成只提交「我改了哪几项」，我没碰过的一律不动。
- **存不下的时候会告诉你**。以前保存附属数据失败没有任何人接住，界面照样显示成功，要等刷新之后才发现改动没了。
- 修复：扩展重新加载那一瞬间，后台偶尔会漏掉书签事件监听（浏览器接口还没就绪），那一轮里你在别处改的书签同步侧看不见。
- 新增 9 项测试，共 183 项通过。

## v0.10.21 · 本地开发

- **说明分成两层**：「一句话说明」还是首页卡片上显示的那句；新增「详细说明」，写什么场景下会用到、要不要一直留着——只在详情页看得到，想写多长写多长。AI 也能读能写这一栏，改动照样走预览、能撤销。
- **详情页顶上多了一排大按钮**：▲上移 ▼下移 ⇤移出 ⇥收进。卡片上那个小箭头怕误触的话，用这排——按钮大、而且动不了的方向直接是灰的。
- 文件夹标题栏上那四个按钮做大了一号。
- 修复：上一版给多家 AI 接口加的「用到哪家才问哪家」授权，实际上**一次都成功不了**。浏览器只在你点击那一下允许申请权限，而代码在申请之前先查了一次「有没有授权过」——就这一下，点击的授权就过期了。现在点完直接申请。
- 新增 2 项测试，共 174 项通过。

### 顺带确认一件你可能担心的事

**删掉书签，你写的备注名、说明、标签不会丢。** 它们按网址存着，下次把同一个网址重新加回来，这些自动回来（网址带不带锚点、结尾有没有斜杠都认得）。实测走了一遍：建→写备注→删掉→重新加（书签名故意写成别的），首页显示的仍然是你起的那个名字。

## v0.10.20 · 本地开发

- **书签和文件夹都能一格一格地挪了**，不用再拖。鼠标移到书签卡片上，右边那条竖带会浮出 ▲▼ 两个按钮，点一下往前/往后挪一格。文件夹的标题栏上也有同样的一组。
- **还能调层级**：⇤ 把它移出当前文件夹（变成父文件夹的同级，紧跟在父文件夹后面）· ⇥ 把它收进紧挨着上面那个文件夹里。书签的这两个动作在右键菜单里，文件夹的直接在标题栏上。
- 动不了的方向会说清楚为什么（「已经是第一个了」「上面紧挨着的不是文件夹，没法收进去」），右键菜单里干脆不列出来——不会让你点了才发现没反应。锁定的文件夹照旧挪不动。
- **左侧目录改成点开才展开**。以前是滚到哪个文件夹就自动把它的子文件夹全铺出来，子夹一多，左栏突然拉长、位置还跟着跳。现在一级文件夹前面有个三角，自己点开；展开的子级往右缩进一层并用竖线串起来，一眼看得出层级。
- 新增 8 项测试，共 172 项通过。其中专门盯着一个容易写错的地方：下移时的目标位置要 +2 而不是 +1，因为浏览器是先把它抽出来再插回去的。

## v0.10.19 · 本地开发

接着上一版把重试这件事做完整——原来两头都不对：失败后要么每分钟砸一次云端、要么干脆再也不试。

- **重试改成越试越慢**：1 分钟 → 2 → 4 → 8 → 16 → 30 分钟封顶。真的只是抖一下，一两次就过去了，你根本不会看见。
- **连着试不成会告诉你**：累计约一小时还同步不上，就转成「同步要你处理」并暂停自动同步，错误里写明「连续 N 次都没能同步成功」，而不是只报最后一次的原因。
  —— 「会自动重试」不能变成永久借口：密码改了、云端停服这种情况，再试一万次也没用，得摆到台面上。
- **成功一次就把连败次数清零**，不会出现「早就好了却还被判死」。

新增 4 项测试，共 164 项通过。

## v0.10.18 · 本地开发

修掉「同步失败，每次都这样」：

- **一次网络抖动，自动同步就永久停摆**。原来任何一次失败——哪怕只是云端一瞬间没连上——都会把自动同步关掉，而它自己永远不会再打开。于是顶栏那颗药丸从此一直挂着「同步失败」，非要你手动成功一次才消。现在把失败分成两类：
  - **会自己好的**（没连上、云端一时没响应、别的任务正在改书签）→ 自动同步继续开着，过几分钟自己再试，药丸显示「上次没连上 · 会自动重试」，点一下就能立刻手动试。
  - **要你拿主意的**（两边改了同一条、第一次连云端、一次删太多、读回核验对不上）→ 才暂停下来等你，药丸显示「同步要你处理 · 点开查看」。
- **已经被卡住的会自己恢复**。升级后第一次自动检查时，如果发现「自动同步是关的、挂着一条会自己好的错、而且以前确实同步成功过」，就把它打开重试——那种状态一定是被失败关掉的，不是你自己关的。实测：一台卡了四个小时的浏览器，开关打回去后二十多秒就自己同步成功了。
- **「立即同步 · 冲突保留云端版本」这个按钮名改了**。它是一个按钮、一次点击，但读起来像是要先点「立即同步」再点「保留冲突版本」。现在就叫「立即同步」，底下写清楚：这一个按钮就够了，只有同一条书签两边都改过、内容还不一样时才算冲突，那种才保留云端那份。旁边的「高级：查看详细差异」改叫「先看看会改哪些（可逐条勾选）」。

新增 9 项测试，共 160 项通过。

## v0.10.17 · 本地开发

独立核实官反查上一版的复查结论，推翻了两条、又自己揪出四条谁都没提的。都修了：

- **写入权限会在 5 分钟后自己失效，而没有人续期**。任何跑满 5 分钟的批次（AI 整理、恢复重建整棵树），权限到期就自动放行，下一次自动任务读到「没人在写」直接开工——这时候那一边还在写。不是概率问题，是必然。现在批次期间每分钟续一次。
- **在 AI 执行到一半时关掉页面，权限不会被交还**，同步和备份要一直等到它自然过期（最长 5 分钟）。现在关页面那一下会把权限还回去。
- **本机备份容量数错了单位**：数的是字符数，而中文书签的真实体积是它的 1.37 倍（全中文标题可达 1.71 倍），于是那道「不超过 6 百万」的闸门实际放行了将近 10 MB，几乎吃光浏览器给插件的全部空间。现在按真实字节算，并给其他数据留出余量。
- **单份备份自己就超标时，那道闸门完全不生效**——它只在「还剩超过一份」时才裁，于是超大的那一份被原样写进去、撑爆存储。现在会挡下来并说清楚原因。
- **存储写满时，界面上什么都看不到**。连「备份失败」这条记录本身都写不进去，错误只落在开发者控制台。现在会退回去写一条精简的失败说明，并在备份页显示。
- **慢操作会被当成失败重发一遍**。书签多的时候备份预览本来就要十几秒，超过 20 秒就被判定「后台没有响应」并原样重发——而它其实还在跑。现在会改东西的操作一律不重发，提示也改成实话：「后台还在处理，请稍等，不要重复点击，也不要停用插件」（原来那句让人去停用插件，反而会打断正在进行的操作）。

订正上一版：`write-lease.js` 里那句「回读一次就不会两边都拿到锁」是错的，核实官用可控交错跑出了反例。注释已改成它真实的样子——今天不出事靠的是「没有任何页面直接用这把锁」，不是靠代码拦住了。

新增 7 项测试，共 151 项通过。

## v0.10.16 · 本地开发

派了五路 AI 通盘复查全部代码，验证属实的问题一次修掉。按会不会丢东西排：

- **导入附属数据会把文件夹锁定和文件夹说明丢掉**。导出本来就带着这两样，导入却只认备注、组颜色和标签——导出再导入、或者拷到另一台，锁和说明就没了，提示还说「已导入」。现在一并带回来，提示里也报清楚各带回了几条。
- **新设备第一次连上已有云端时，锁定和文件夹说明会凭空消失**。首次同步会把本机的文件夹身份对齐到云端那一套，而锁和说明是按身份存的，对齐时漏了它们——数据还在文件里，但谁也对不上。折叠状态早就处理了，这两样现在跟它一致。
- **恢复过程中 AI 仍能同时改书签**。恢复只检查「别人有没有在写」，自己却从不占位；现在恢复全程持有写入权限，结束或失败都会释放。
- **拿不到写入权限的一方，反而会把正在写的那一方踢掉**。释放时没拿到凭据就直接清空，正好放走了这把锁要防的那件事。现在没有凭据一律拒绝释放。
- **锁定的文件夹里，重复收藏仍能从详情侧栏删掉**。锁早就改成按文件夹身份记了，这一处还只认旧的按名字锁。两种现在都认。
- **AI 给文件夹改名会丢掉颜色**。颜色是按文件夹名字存的，改名不带着走就没了，旧数据还留在旧名字下。现在跟着搬，也能撤销。
- **标签颜色不再原样塞进页面**。标签能从「导入附属数据」和 AI 新增两条路进来，这两条都不经过备份那套校验；现在只认十六进制颜色，别的退回调色板。
- **AI 新增标签时，说明会显示在预览里了**。那段说明会进每一轮给模型的提示并跟着同步走，原来预览只显示标签名，看不见就点了执行。
- 新加的那 9 家 AI 接口补上了访问权限——实测火山方舟、腾讯混元、讯飞星火三家不放行跨域，没权限连鉴权都到不了。权限改成**用到哪家才问哪家**，装扩展时不会为你没用的服务索权。
- 新增 10 项针对性回归测试，共 144 项通过。

## v0.10.15 · 本地开发

- 接口那一段的字整体放大一号，输入框和下拉也加高了，不用眯着眼看。
- 去掉「温度」那一格——整理书签只需要稳，固定用最稳的那一档；会思考的模型本来就不吃这个参数，摆出来只会让人以为它有用。
- 选完模型，底下直接给两个大按钮：「去 ×× 注册 / 拿钥匙」和「查价格和免费额度」，点了就跳到那一家的控制台，不用自己找。
- 顶栏三种长相统一成一种椭圆药丸：AI 那颗左半是设置、右半进侧栏；「备份与恢复」和同步状态合成一颗——本来就是同一页，没连同步时显示「☁ 备份与恢复」，连了就显示云端状态。
- 顺带修好：同步药丸的状态配色原来挂在旧的样式名上，换长相时会整片掉色。现在按元素身份写，跟长相解耦。

## v0.10.14 · 本地开发

- 接口从 Kimi 一家扩到 **12 家**：Kimi（会员和开放平台）· DeepSeek · 智谱 GLM · 硅基流动 · 阿里通义 · 火山豆包 · 腾讯混元 · OpenRouter · 百度千帆 · 讯飞星火，外加一个**自定义**（任何 OpenAI 兼容接口，本地 Ollama 也行）。
- 每家都带「去拿钥匙」和「查价格和额度」两个直达链接；官方标长期免费的那一档在模型名后面标「★免费档」——想先不花钱试，选智谱 `glm-4.7-flash`、腾讯 `hunyuan-lite` 或讯飞 `lite`。
- 🔴 **不写额度数字和单价**：那种东西写下来第二天就在骗人，而且不报错。要数字点链接过去看。有一项测试专门盯着这条，防以后手滑写回去。
- 接口地址是逐个实测过的：不带钥匙发一次请求、返回 401（地址对、只是没授权）才收进来，脚本留在 `dev/probe-api.py`，以后可以重跑复验。
- 会思考的模型（k3、deepseek-reasoner、带 thinking 的）自动不送温度参数，不用自己记哪个能调。
- 修复：点顶栏「且 / 或」那个切换，每点一次就往本机存储里写一个名叫 `undefined` 的垃圾键，还会被带进备份文件。现在不写了，已有的那个也会自动清掉。
- 接口配置抽成一份 `ai-providers.js`，首页、侧栏、设置页共用，不再各存一份。新增 6 项测试，共 134 项通过。

## v0.10.13 · 本地开发

- 新增「AI 整理标准」页（顶栏 AI 左边那个 ⚙）。在这儿把「我的书签该怎么整理」写清楚，写完之后侧栏里的 AI **每次对话都带上它**，不用每次重新交代一遍。
- 页面分四段：**整理标准**（长期规矩，每次都带）· **常用任务**（一次性的活，写好就是侧栏那排按钮，可增删改排序）· **范围与安全**（说清 AI 能动什么、动不了什么）· **接口与钥匙**（原来那个弹窗搬过来了）。
- 顶栏 AI 变成左右两个按钮：左边 ⚙ 进设置页，右边进侧栏对话。
- 三层规矩各管一段，互不重复：**整理标准**管全局 · **文件夹说明**管这一个夹该放什么 · **选中文件夹**管这次只动哪一摊。
- 整理标准和常用任务跟着完整备份走（换台机器、恢复版本都不会丢），但**不进同步内容**——每台可以有自己的标准，不会互相打架。钥匙两样都不进。
- 新增 1 项测试专盯这条边界，共 128 项通过。

## v0.10.12 · 本地开发

- 顶栏重新分工：第一行只放主要功能——搜索、AI、备份与恢复、同步状态；第二行放怎么看（卡片/列表）、新分组、整理文件夹和标签组。
- 顶栏那条「跟 Kimi 说怎么整理书签」的输入框撤掉了，换成一个「✦ AI」按钮：点一下右侧栏直接开到 AI 页。对话、粘网址入库、改动预览全在侧栏里，不再挤在顶上的浮层。
- 搜索框里「（/ 聚焦）」去掉了，那是写给自己看的。按 `/` 跳到搜索框这个快捷键没变，说明挪进了鼠标悬停提示。
- 设成「只接收，不上传」的浏览器，首页同步状态直接显示「只接收」，不再报「本机待上传」——那台本来就不会上传，报了反而吓人。

## v0.10.11 · 本地开发

- 还没升级的浏览器不会再把新功能的数据悄悄抹掉。旧版本写云端时根本不带「文件夹锁定」和「文件夹说明」这两样，原来会被当成「用户删掉了」传播开，救不回来。现在分得清「它不认识这个键」和「你把它清空了」——前者保住你的数据，后者照常传播。这条不需要旧版本配合，今天就生效。
- 每台浏览器可以单独设成「只接收，不上传」。在一台机器上整理、其余几台只跟着走时勾上：它每次同步以云端为准，自己的改动不上传也不产生冲突。（会用云端盖掉本机改动，所以别在你平时整理书签的那台勾。）
- 新增 8 项测试，共 127 项通过。其中「只接收」那条是用两个模拟浏览器对着一个模拟坚果云跑的，断言它一次云端写入都没发。

## v0.10.10 · 本地开发

- 每个一级文件夹的标题栏上多了一个「说明」：写清楚这个夹该放哪类内容、哪类不放。写完之后它就是规矩——AI 归类时会照着办，发现夹里的东西跟说明对不上还会说出来。
- 说明里那个「让 AI 看着写一条」：先把工作范围设成这个夹，再让它只看这一摊的内容起草，起草完走预览，你点执行才写进去，也能整批撤销。
- 说明按文件夹的稳定标识存，跟锁定同一套——改名不会丢，两个同名夹也不会互相串。
- 说明跟着多设备同步走，两端各写各的会合并，一端删掉会传播。（上一次漏掉「锁定」就是每同步一次全丢一次，这次一起补了回归测试。）
- 新增 7 项测试，共 119 项通过。

## v0.10.9 · 本地开发

- 左边点一个文件夹，右边的 AI 就只在这一摊里干活（VS Code 那个意思：左边选，右边做）。被选中的夹在左栏标出来，对话区顶上出现一张卡：哪个夹、含几个子夹、多少条、「这次对话只在这一摊里改」。再点同一个夹就取消，或者按卡片上的叉。
- 范围首页和侧栏共用一份，哪边改了两边都跟上；关掉浏览器就没了，不会留到下次。
- 含子夹，并且不给开关——你在左边点的是「这一摊」不是「这一层」。条数和子夹数写在卡片上，看得见它到底圈了多少。
- 范围真的拦得住，不是只写在提示词里：
  - 读的三个工具只返回范围内的东西，搜索会告诉它「范围外还藏了 N 条」；
  - 全局概览降级成骨架——范围外的文件夹只报名字和条数，书签标题和网址一条都不给；
  - 提交范围外的改动整批挡回，并指名道姓说是哪一条越界（跨夹移动时，目标夹也必须在范围里）；
  - 被挡回时给的是结构化回复加唯一出路（请用户把那个夹也附加进来），不写「请重试」，免得它换个工具再试一遍。
- 发出去的每一句都盖上当时的范围：已经发过的内容收不回来，所以直接标在那条消息上。
- 范围里那个夹如果被删了，会当场恢复成全部书签，不会变成「什么都办不了又说不清为什么」。
- 顺带修好侧栏 AI 区四个取不到值的颜色变量（上一版从首页搬样式时漏带，导致卡片背景和边框是透明的）。
- 新增 8 项测试，共 112 项通过。

## v0.10.8 · 本地开发

- 「所在文件夹」原来在下拉框底下把同一句路径又写了一遍，看不出有什么用，去掉了。
- 新增「同一个网站的收藏」：一眼看到这个网站一共收了多少条、散在几个文件夹里，每个文件夹多少条，当前这份在哪个夹里也标出来。点开任意一行看明细——是哪些页、属于哪个子域名，可以直接跳过去看那一份。
- 按注册域归并：`cc.xntj.tv` 和 `docs.xntj.tv` 算同一个网站，子域名只在明细里区分。这样同一个站散落在各处的收藏能一次看全，再按主题决定哪些留、哪些不要。
- 散得多的网站（实测有的站分在 44 个文件夹里）默认只列前 8 个夹，一按就全部展开，不会把详情页撑得老长。
- 新增 7 项测试，共 104 项通过；测试里加载后台脚本的名单改成直接读 `background.js`，不再手抄一份（抄漏时报的错看不出是名单没跟上）。

## v0.10.7 · 本地开发

- 修复：侧栏「详情」整页拉不下去，「保存修改」和「收起」被裁在看不见的地方。上一版给侧栏加两个页签时，中间多了一层不参与纵向伸缩的容器，内容一高就被整段裁掉，而且哪里都滚不动。现在内容区自己滚，按钮一行始终贴在底部。
- 修复：在 AI 页打开「Kimi 设置」后切回详情，弹窗留在打开状态，整页点不动也滚不动（这类弹窗会让页面其余部分失效）。现在离开 AI 页时自动关掉它。
- 「书签名」下面写清楚了：那是收藏时网页自己带过来的标题，保持原样就行，想改首页上的叫法请改「显示名」。
- 底部那栏原来叫「图标与更多操作」，看不出来是干什么的，改成「更多操作：换个图标 · 删掉这条书签」；里面的「自定义图标」改叫「换个图标」并说明它换的是首页上那个小方块。

## v0.10.6 · 本地开发

- 侧栏分成「详情」和「AI」两个页签，在同一列里切换，记住上次停在哪一页；从书签卡片箭头进入时自动回到详情。
- AI 搬进侧栏：整列的对话区、快捷任务和改动预览都在这里，长内容不再挤在顶栏的浮层里。首页顶栏保留输入框，另有按钮直接把侧栏打开到 AI 页。
- 修复：粘网址入库后，AI 补备注名的提示会把「撤销」按钮顶掉，导致撤不回来。现在无论补说明成功还是失败，撤销始终可用，撤销时连同自动补的备注一起清掉。
- 修复：多设备同步时会把文件夹锁定整个丢掉——附属数据合并只重建了备注、分组和标签，没有带上锁定。现在一并合并，一端解锁也会正确传播。
- 书签树与附属数据的派生逻辑抽到 `bm-core.js`，首页和侧栏共用同一份，避免两处规则各自演变。
- 新增 11 项测试，共 97 项通过。

## v0.10.5 · 本地开发

- 同一时刻只允许一方成批修改书签：AI 整理、持续同步、恢复之间改为互相避让，不再彼此打断后留下半新半旧的书签。被占用时给出是谁在改、稍后再试，而不是失败。
- 这个占用标记带有效期，页面或后台中途退出也会自动失效，不会永久卡住自动任务。占用期间不丢弃任何书签事件，你自己的手工修改和浏览器账号同步照常识别。
- 锁定改为记在文件夹的稳定标识上：重命名后依然锁着，同名文件夹不再互相影响。原有按名称的锁在名称唯一时自动迁移，同名多个时保持原样并继续生效。
- 与后台的所有通信统一加了超时与一次重试，后台未响应时明确说明「这次操作没有执行」以及如何恢复，不再无限等待或停在「正在读取…」。
- 删除书签时的保护副本改为每批只留一份（90 秒内的连续删除视为同一批），不再每删一条就完整备份一次。
- 新增 6 项写入协调测试，共 86 项测试通过。

## v0.10.4 · 本地开发

- AI 执行一批改动后可以整批撤销：执行时逐条记下逆操作，撤销时倒序重放，只碰这批动过的书签，不影响期间的其他修改，也不需要整树恢复。
- 支持撤销的操作：新建、改名、改地址、移动、排序、删除（含整个子文件夹连同内部书签一起还原）、备注与标签、分组颜色。
- 撤销前逐条核对现状：这期间被你自己改过的、或已经不在的，跳过并说明原因，不覆盖你的修改。撤销结果会写明成功几项、几项没能撤销以及各自原因。
- 删除前的子树快照改为从书签数据实读，不再使用可能过期的页面缓存，避免还原出来的名称或结构与删除那一刻不一致。
- 重放逻辑抽到 `ai-core.js`，可在浏览器外用模拟书签树测试；新增 10 项测试，覆盖「先移动再删除父文件夹」这类交叉依赖、被他人改动后的跳过、排序还原等。共 80 项测试通过。

## v0.10.3 · 本地开发

- AI 会话历史改为按「完整回合」裁剪：切点只落在下一条用户消息之前。原来按条数裁剪会把带 `tool_calls` 的回复和配对的工具结果切成两半，下一次请求直接失败；k3 的思考内容也会跟着它那条回复一起保留或一起丢弃。
- 顶栏 AI 输入框：整段都是网址时直接加进书签，不再进入对话——有 Inbox 就放 Inbox，否则放书签栏末尾，并给出一次性撤销。混了其他文字仍然走对话。
- 配好钥匙时，入库后由 AI 补短名和一句话说明，只写备注这类附属数据，不移动书签、不改书签树；没配钥匙也能正常入库。
- 新增 `ai-core.js` 存放可在浏览器外单测的纯逻辑，并补 7 项测试（含 200 轮随机的配对不变量、以及一条证明旧写法确实会切断配对的对照测试）。共 70 项测试通过。

## v0.10.2 · 本地开发

- 备份页初始化改为「先回填本机已保存配置，再查远程状态」：同步状态、最新版和倒计时各自失败只降级本段，不再让整页停在「正在读取…」。
- 没读到本机配置时，保存方案、连接坚果云、移除连接、立即备份、立即同步等写入按钮一律禁用，并说明「配置仍在，请重新加载本页」；表单原值和未保存草稿都保留，避免把暂时空白当成未配置再保存一次空值。
- 扩展重载后旧页面的 chrome.storage 会消失，相关读写改为可失败：初始化链不再因同步抛出而中断。
- 目录高亮不再「滚到页底就选最后一段」，改为按段标题越过判定线判定；点目录后短暂锁定高亮，等平滑滚动落定再交还。正文底部留白，保证最后一段也能滚到判定线。
- 首页同步状态条由「打开设置页」改为主动检查：依次显示检查云端、上传中／下载合并中、读回核验中、已核验和失败原因并带时间；确认云端已是最新版时不触发任何写入。真正的同步仍走既有流程（冲突保留云端、先存保护副本、写后完整读回核验）。设置入口保留在旁边的「备份与恢复」。
- 状态条不再依赖 APP_READY 落定，后台冷启动或无响应时显示读取中并重试，超时给出明确结论而不是静默消失。
- 新增两项目录高亮回归测试；全部 63 项测试通过。

## v0.10.1 · 2026-09-13

- 正式 Chrome 与 Edge 实测同步到同一云端版本，书签与元数据内容指纹一致；Edge 中断恢复通过。
- 开发流程统一沿 main 推进，已合并临时分支清理，版本标签保留发布节点。

- 默认一键同步，成功后自动开启持续同步；字段冲突采用云端结果，详细差异改为高级入口。
- 中断恢复可直接采用云端共同版本，写入前保留本机恢复快照。
- 兼容 Edge 自动转换书签管理页网址，保留逻辑地址避免核验失败和反复上传。

- 冲突预览增加批量选择和网址打开入口，使用更明确的本机 / 坚果云文案。
- 详情侧栏增加完整网址重复清单、路径、打开与查看详情、单份删除及撤销；删除保留共享备注。
- 同步中断增加不含网址和标题的事件诊断，继续保留并发保护。
- 公开新浏览器整批接入研究方案、恢复边界和验收清单；该接入流程尚未实现。

- 补充 Chrome / Edge 账号同步开关、与坚果云同步的区别，以及连接、备份核验、同步核验和中断状态说明。

## v0.10.0 · 2026-09-13

- 同步备份页面拆成六个独立可折叠区域，标题和目录同步显示真实状态。
- 保留整高侧栏，点击目录自动展开并滚到对应位置，支持地址锚点。
- 根据配置进度提供初始展开状态；用户手动选择保存在本机。
- 页头增加同步核验概览、下一步入口，以及展开全部和收起全部。
- 窄屏使用横向导航，摘要换行；异常提示与恢复保护入口持续可见。

## v0.9.9 · 2026-09-13

- 备份页左侧目录延伸到视口底部，返回首页入口固定在底部。

## v0.9.8 · 2026-09-13

- 备份页增加目录、滚动高亮及窄屏横向导航。

## v0.9.7 · 2026-09-13

- 坚果云 HEAD 缺少 ETag 时退回 GET，保留条件写入与完整核验。

## v0.9.6 · 本地开发

- 持续同步改为每分钟检查一次，设备间随机错开 5 到 25 秒；无变化时只读取 WebDAV 版本标识，避免高频下载完整书签文件。
- 首页和同步页显示“已是最新版、云端有新版、本机待上传、等待合并、同步待处理”等状态。
- 共享同步版本记录上一个版本、更新时间、更新设备与内容指纹；发现新版后仍通过三方合并和条件写入防止覆盖。

## v0.9.5 · 本地开发

- 界面中文名称统一为「书签首页」。
- 自动备份、持续同步和云备份重试分别保存本机下次执行时间，每轮随机错峰 30 秒到 5 分钟。
- 备份页显示实时倒计时、计划时间及关闭或暂停状态；重开页面不重置。
- 休眠错过计划后重新错峰补做，后台使用单次定时触发并保留 5 分钟兜底检查。
- 根分组到四层文件夹统一使用同一列数，保留每层 20px 缩进；4K 屏幕按宽度增加到最多 12 列。
- 顶栏时间区域靠到窗口最右侧。

## v0.9.4 · 2026-09-13

- 修复定时备份失败后跳过持续同步的问题；两项仍串行运行，同步仍须通过自己的保护备份与冲突检查。
- 覆盖安装、启动、新标签页和定时检查四种入口，备份错误继续保留并显示。
- 连接坚果云不再覆盖久置表单中的备份频率与设备名，方案草稿单独保存；方案保存时核对加载时的设置，拒绝覆盖其他页面的新修改。
- 未完成恢复或同步时明确提示自动任务暂停，并提供查看保护副本和重新预览入口。
- 同步成功回执与解除未完成标记合并为一次本机存储写入。

## v0.9.3 · 2026-09-13

- 新增每小时、每 3 小时自动备份，兼容已保存的天数频率；新安装默认每小时。
- 云备份与同步增加每次读回核验，失败不记录新的成功时间；同步回执展示云端版本、内容指纹和两端变更数量。
- 明确当前浏览器核验与其他设备接收的区别，增加核验状态刷新入口和扩展版本显示。
- 导入备份改用标准按钮，支持键盘与辅助功能操作。

- 坚果云配置明确展示账号与应用密码的保存状态，解释密码留空仍可复用已保存密码。
- 打开备份页优先展示坚果云，保留实际备份开关；扩展重载导致页面连接失效时提供重新加载提示。

- 默认生成并保存无日期的随机设备名，保留已有自定义名；改名仅影响未来备份，设备标识保持不变。
- 在同步页补充设备改名、旧备份名称和恢复时的行为说明。

- 同步、备份与恢复页增加多设备配置对照表与首次操作步骤，说明共用账号和完整目录，设备名称只标注来源。
- 将同步与备份目录改为直接展示，并补充仅备份、自动同步、下载与恢复的区别。

- 备份页常驻浏览器书签同步设置入口，直达 Chrome「管理您的同步数据」。
- 明确首次上传、下载合并、历史恢复及两种同步并存的区别。

- 适配真实坚果云：使用原样版本标识及无缓存请求；首次创建用 MOVE 禁止覆盖，目标竞争返回 409 / 412 时保留原文件。
- 区分账号目录验证、完整备份上传和同步能力验证；通过测试不会自动执行合并。
- 修复无结尾斜杠的月份目录识别，以及 JSON 字段顺序不同造成的备份重试误报。
- 同步验证记录文件读回校验和清理结果；应用密码获取步骤简化为官网入口。

- 直接通过坚果云双向同步书签、备注、标签与颜色，无需安装 floccus；浏览器账号同步可保留。
- 使用共同基线三方合并，首次保留双方；字段冲突、疑似回流或大量删除暂停自动同步。
- 同步连接验证条件写入能力，使用强 ETag 防止多设备覆盖，未完成任务支持重新合并。

- 重做备份与恢复独立页面：坚果云、浏览器账号、纯本地三种方案单选。
- 加入坚果云注册、应用密码、连接验证、目录创建与签屿直接同步引导。
- 本机自动备份支持每小时、3 小时、每天、7 天、30 天；纯本地停止自动备份与上传，已有数据保留。
- 云快照按月存放，支持月份选择、旧目录兼容、下载与恢复预览。
- 云上传失败可重试，已有同名文件核对内容后处理，避免覆盖；断网不阻止定时本机备份。
- 新备份包含设备名称和可跨设备恢复的文件夹折叠状态。
- 恢复自动暂停签屿同步，并提示浏览器账号同步的传播影响；本机历史可单独清理，未完成恢复时保护副本不可清空。

- 将带「废弃」标签的书签和文件夹稳定排到同级末尾，覆盖首页、导航、整理页与搜索。
- 文件夹右键支持标记和取消废弃；搜索、筛选识别所属文件夹的废弃状态。
- 各级文件夹支持独立折叠并记住本机状态；已有 Inbox 高亮显示「收纳」，其内部默认折叠。
- 层级标记改为单条竖线按级数分段，应用到主区域、导航与整理页。
- 导航定位和标签筛选临时展开相关内容，搜索仍覆盖折叠书签。
- 子文件夹支持在标题上、下缘拖动排序，以及移入已收起的文件夹。
- 左侧网站 logo 支持点击复制完整网址，覆盖首页、搜索结果和最近访问。
- 复制成功显示勾号与提示，失败时明确提示；支持键盘回车和空格操作。

## v0.9.2 · 2026-09-13 · 首个公开版本

以当前可用代码建立公开起点，不包含此前的本地 Git 历史。

- 使用浏览器书签栏生成新标签页，支持搜索、标签、分组颜色和同主域高亮。
- 保留多层文件夹，固定缩进并显示层级竖线，支持拖动整理。
- 按需展开 Chrome 原生详情侧栏，同一书签箭头再次点击即可收起。
- 紧凑排列网址、书签名、显示名与备注，支持彩色标签和编辑草稿。
- 提供本机历史、JSON 导入导出、恢复预览与恢复前保护。
- 可选使用坚果云 WebDAV 保存独立历史快照。
- 补充安装说明、数据边界、贡献流程和 MIT 许可证。

AI 功能仍属实验入口；真实云端上传需要在用户自己的账号环境中验证。
