# 书签首页（Chrome 扩展）

新标签页把浏览器书签栏摊开成文件夹和链接卡片。
书签位置、名称和网址直接读写浏览器书签，floccus 等同步工具可以继续使用。

需要 Chrome 141 或更新版本。

## 安装

```text
chrome://extensions
```

打开扩展管理页，开启开发者模式，选择「加载已解压的扩展程序」，选中本项目目录。
如果还装着其他接管新标签页的扩展，需要停用它们。

----------------

## v0.9.2：按需打开详情侧栏

点击链接左侧 logo 复制完整网址，成功后显示勾号与提示；首页、搜索结果和最近访问中均可使用。
也可以用 Tab 聚焦 logo，按回车或空格复制。点击名称仍然打开网页。

点击链接卡片右端的 ›，或右键选择「详情 / 编辑」。
Chrome 原生侧栏会展开，主页随之缩窄；关闭侧栏后，主页恢复可用宽度。
平时没有常驻详情栏，也不会自动显示默认书签。

再点同一条书签的箭头，侧栏就会收起；点击另一条书签的箭头，则切换详情。
展开中的箭头会反向显示，提示再次点击可以关闭。

侧栏依次展示网址、书签名、显示与备注、所在文件夹。
书签名使用紧凑的单行输入框，并注明它是收藏网页时带入的标题。
显示名只影响首页展示，建议使用 4～6 个字，不会截断或覆盖完整书签名。
标签沿用首页的自定义颜色，选中后加深底色和描边。
图标和删除等较少使用的操作收在「图标与更多操作」里。
保存状态、保存按钮和「收起」固定在底部，滚动编辑长内容时仍可操作。

书签名、网址和所在文件夹需要点击保存。
备注名、说明、图标和标签会自动保存。

尚未保存的书签修改会保留为草稿。
在本次浏览器会话内，关闭侧栏或切换到其他书签后，可以再打开继续编辑。
「放弃草稿」重置未保存的书签字段，不撤销已自动保存的备注和说明。

如果其他地方修改了同一条书签，保存时会提示重新载入，避免覆盖对方的修改。

## 多层文件夹

保留原有层级，不压平已有文件夹。

中间内容区每深入一级向右缩进 20 像素。
标题前的竖线按层级配色：一级蓝、二级绿、三级橙、四级紫。
第五级起重复颜色，同时显示实际级数。
分组自定义颜色继续用于标题色块和底色。

左侧导航只列前两级，更深层在中间内容区查看。

整理文件夹页中，点击「第3级及以下」展开深层文件夹。
拖到文件夹左右两侧调整顺序，拖到右端的 ↳ 放入该文件夹。
不限制层数，但不能放进自身或自己的后代。

----------------

## 备份与恢复

点击工具栏「备份与恢复」。

完整备份包含：书签栏的书签与文件夹、名称与网址、顺序、备注、说明、标签、图标、分组颜色和显示偏好。
其他书签根目录不在此次备份和恢复范围内。
AI Key、坚果云应用密码和编辑草稿不会进入备份文件。

### 本机历史

默认每天备份一次；浏览器启动和打开书签首页时会检查是否需要补备份。
也可以点击「立即备份」。

本机最多保存最近 20 份，超过约 6 MB 时会先移除最旧的版本。
本机备份存在扩展存储里；卸载扩展或删除浏览器资料会丢失它们。
请下载 JSON，或使用坚果云保存浏览器以外的副本。

### 恢复

从本机历史、导入的 JSON 或云端历史中选择「预览恢复」。
先查看新增、删除、改名、改地址、移动和排序清单，再确认恢复。

恢复前会自动保存当前完整状态。
尽量复用仍存在的书签，保留它们的浏览器标识和创建日期。
已经删除、需要重新创建的书签，会获得新的浏览器创建日期。

如果预览后书签又有变化，需要重新预览。
如果恢复中断，页面会提示从「恢复前自动保护」的备份恢复。

### 坚果云（可选）

在坚果云账户信息的「安全选项 → 第三方应用管理」中创建应用密码。
在备份页填写账号、应用密码和专用目录，例如：

```text
https://dav.jianguoyun.com/dav/书签首页备份/
```

这是存放快照的独立目录。
保存设置时，Chrome 会请求访问坚果云的权限。
然后点击「连接并读取历史」验证连接。

勾选「同时上传」后，手动和每日自动备份会上传到这个目录。
每个文件名包含时间和唯一标识，不覆盖旧文件，也不修改 floccus 的同步文件。
云端旧快照不会自动清理。

应用密码只存在本机扩展存储里，不会写进 Git 或导出的备份。
当前版本仅支持坚果云的官方 WebDAV 地址。

----------------

## 数据分别存在哪里

| 数据 | 存储位置 | 同步方式 |
|---|---|---|
| 书签名称、网址、顺序、归组 | 浏览器书签栏 | 跟随浏览器或 floccus 的书签同步 |
| 备注名、说明、标签、图标、组颜色 | 扩展本机存储 | 附属数据导入导出，或完整备份恢复 |
| 显示偏好 | 扩展本机存储 | 完整备份恢复 |
| 历史快照 | 扩展本机存储，可选坚果云 | 下载 JSON 或云端上传 |

旧的「⋯ → 导入／导出附属数据」入口仍保留。
它只迁移显示信息，不恢复书签树。

## 开发验证

```sh
node --test tests/core.test.cjs
```

测试使用模拟书签和模拟网络，验证恢复、草稿、冲突和云端请求，不修改真实书签。

当前源代码包含浏览器外的内存模拟数据层。
完整的离线预览需要自行提供模拟书签数据；仓库不提交真实书签副本。
