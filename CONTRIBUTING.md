# 参与 TabIsle

欢迎提出问题、改善界面和贡献代码。请在问题说明中写清 Chrome 版本、复现步骤、预期与实际结果。
截图和测试文件请先去掉个人书签、账号及其他私人信息。

----------------

## 本地开发

使用 Chrome 141 或更新版本，通过「加载已解压的扩展程序」加载项目目录。
不需要安装依赖或运行构建。

建议在独立的浏览器资料目录中测试，并使用虚构书签。
文件夹移动、删除、恢复和同步测试可能影响真实书签及其他设备。

```sh
node --test tests/*.test.cjs
```

这条命令运行核心行为测试，覆盖备份恢复、草稿、冲突以及模拟 WebDAV 请求。
涉及界面或 Chrome API 的变更，还需要在浏览器中验证对应交互。

## 分支和提交

主分支使用 `main`，保留已验证的代码。
功能或修复使用短期分支，完成后通过 Pull Request 合并；纯文档等小改动可以直接提交主分支。

```sh
git switch -c feat/bookmark-search
```

这条命令创建一个功能分支。修复分支可以使用 `fix/` 前缀。

每次提交围绕一件完整的事，使用下面的前缀，后面用中文或英文写清结果。

| 前缀 | 用途 |
| --- | --- |
| `feat:` | 新增功能 |
| `fix:` | 修复问题 |
| `docs:` | 说明文档 |
| `refactor:` | 不改变使用行为的代码整理 |
| `test:` | 测试变更 |
| `chore:` | 开发配置与维护 |

提交前检查差异和暂存文件。不要使用真实用户数据作为测试样本。
代码变更运行相关测试；只修改文档时检查文字、链接和差异即可。

```sh
git diff --check
git diff --cached --stat
```

第一条检查差异中的空白问题，第二条查看即将提交的文件范围。
合并说明写清改变了什么、为什么改变，以及如何验证。

----------------

## 版本与发布

首个公开版本为 `0.9.2`，当前版本为 `0.10.0`。
开发阶段的旧本地历史不导入公开仓库。

修复通常增加末位版本号，新增功能增加中间版本号；不兼容的数据或使用方式变化需要单独说明迁移办法。
纯文档修改不必改变扩展版本号。

发布时统一更新扩展清单、README 和更新记录，并在验证后的提交上创建对应的版本标签。
不要覆盖已发布的标签，也不要强制改写主分支历史。

公开内容只包含代码、文档和虚构测试数据。
个人备份、应用密码、API Key、本机工具和协作记录留在本地；历史中已出现的数据不能仅靠忽略规则移除。
